.header {
    display: flex;
    justify-content: space-between;
    align-items: center;
}
.header__logo {
    font-size: 36px;
    font-weight: 900;
    color: rgb(255, 255, 255);
}
.header__btns {
    margin: 0 20px;
}